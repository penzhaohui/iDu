<odoo>
  <data>
    <!-- explicit list view definition -->
    <!--
    <record model="ir.ui.view" id="coordinator.list">
      <field name="name">coordinator list</field>
      <field name="model">coordinator.coordinator</field>
      <field name="arch" type="xml">
        <tree>
          <field name="name"/>
          <field name="value"/>
          <field name="value2"/>
        </tree>
      </field>
    </record>
    -->

    <!-- actions opening views on models -->
    <!--
    <record model="ir.actions.act_window" id="coordinator.action_window">
      <field name="name">coordinator window</field>
      <field name="res_model">coordinator.coordinator</field>
      <field name="view_mode">tree,form</field>
    </record>
    -->

    <!-- server action to the one above -->
    <!--
    <record model="ir.actions.server" id="coordinator.action_server">
      <field name="name">coordinator server</field>
      <field name="model_id" ref="model_coordinator_coordinator"/>
      <field name="code">
        action = {
          "type": "ir.actions.act_window",
          "view_mode": "tree,form",
          "res_model": self._name,
        }
      </field>
    </record>
    -->

    <!-- Top menu item -->
    <!--
    <menuitem name="coordinator" id="coordinator.menu_root"/>
    -->
    <!-- menu categories -->
    <!--
    <menuitem name="Menu 1" id="coordinator.menu_1" parent="coordinator.menu_root"/>
    <menuitem name="Menu 2" id="coordinator.menu_2" parent="coordinator.menu_root"/>
    -->
    <!-- actions -->
    <!--
    <menuitem name="List" id="coordinator.menu_1_list" parent="coordinator.menu_1"
              action="coordinator.action_window"/>
    <menuitem name="Server to list" id="coordinator" parent="coordinator.menu_2"
              action="coordinator.action_server"/>
    -->
  </data>
</odoo>